# Scotland Driving Companion

Public PWA shell for a private Scotland trip guide.

The itinerary data in this repository is encrypted in `encrypted-data.json`.
The passcode is not stored in the repository.

Open the published GitHub Pages URL on Android Chrome, unlock once with the
passcode, then use **Add to Home screen** / **Install app**. The unlocked guide
is saved on that phone for offline use.

